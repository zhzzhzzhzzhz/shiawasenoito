# 《幸福的丝线》单人模式智能体化升级方案

> 版本：v1.1 ｜ 日期：2026-09-01 ｜ 适用代码基线：`backend-python/`（含 2026-08-16 贝叶斯 AI 升级 + 2026-09-01 范围内标记规则修正）

---

## 0. 背景与现状

### 0.1 游戏规则速览（决策相关部分）

| 要素 | 内容 |
|---|---|
| 棋盘 | 5×5 共 25 名角色（ID 201~605，403 恒为死亡） |
| 阵营 | 3 名反派隐藏在 24 名候选之中；其余为正派 |
| 正派行动 | 每回合（第 1 回合除外）选 **3 个角色放监视标记**，标记当回合生效、回合末失效 |
| 反派行动 | 每回合打 1 张行动卡（卡池 5 张：九宫格×2 / 十字×2 / 混卡×2 / 十字×2+九宫格），放置 ≤ 卡面次数的死亡标记，标记永久保留；**反派必须站在标记影响范围内（2026-09-01 起为引擎层硬规则）** |
| 信息边界 | 正派**看不到任何角色身份**；反派全知。死亡标记目标与形状对双方公开 |
| 胜负 | 正派：3 名反派全部失去行动能力（被监视/带死亡标记）即胜；反派：撑到第 6 回合结算仍有可行动反派即胜 |
| 决策点密度 | 每局正派 5 次决策（回合 2~6）、反派 5 次决策（回合 1~5），共约 10 个决策点 |

### 0.2 现有代码驱动 AI 的资产盘点（升级的"地基"）

| 资产 | 文件 | 智能体化后的角色 |
|---|---|---|
| 贝叶斯反派推断（C(24,3) 候选 + 约束传播 + 几何似然） | `ai/bayes.py` | **保留为智能体的"信念工具"**：给 LLM 提供嫌疑热度图 |
| 正派三档 AI（随机 / 边际 top3 / 一层极小化） | `ai/good_ai.py` | 规则大脑（RuleBrain），充当兜底与基线 |
| 反派三档 AI（随机 / 贪心评分 / 全排列搜索） | `ai/evil_ai.py` + `ai/search.py` | 同上；也是"老师策略"与评测对手 |
| 集中权重配置 | `ai/config.py` | 不变，供回放调参 |
| 对战基准测试 | `benchmark.py` | 扩展为**分阶段评测 harness 的骨架** |
| 接入点 | `services/game_session.py` 的 `ai_good_place()` / `ai_evil_action()` | 智能体的**唯一对接面**（见 §1.4） |
| 已量化基线 | `ai_upgrade_report.md` | L2/L3 阶段的通过线参照（正派 normal 命中率 0.73、hard vs hard 0.56 等） |

> ⚠️ 已知规则层问题（报告结论）：正派胜率几乎为 0（规则使然，非 AI 缺陷）。**智能体升级不解决此问题**，评测指标应以"推理命中率 / 决策质量"为主、胜率为辅，详见 §3。

---

## 一、总体架构与实现方案

### 1.1 设计目标与四条硬原则

1. **零作弊**：智能体的观测严格限定在信息边界内——正派智能体只能看到公开棋盘 + 公开历史，**绝不传入真实反派身份**（沿用 `bayes.py` 的模块级信息边界原则）。
2. **永不卡死对局**：LLM 可能输出非法/幻觉动作，任何失败都要走"重试 → 规则兜底"链，保证对局流程像现在一样鲁棒。
3. **接口零破坏**：前端、路由、房间管理对 AI 的调用方式完全不变，智能体是可插拔的"大脑"。
4. **能力可度量**：每一阶段升级都要能跑出与现有三档 AI 可对比的量化结果（沿用命中率/胜率/耗时口径）。

### 1.2 整体架构：Brain 抽象层 + 三条实现路线并存

在 `GameSession` 与具体决策逻辑之间插入一层 **Brain 抽象**，现有规则 AI 与新智能体平行存在：

```
GameSession (auto_advance)
   ├─ ai_good_place()  ──┐
   └─ ai_evil_action() ──┴──► Brain.good_decision(ctx) / Brain.evil_decision(ctx)
                                  ├─ RuleBrain     包装现有 ai/good_ai.py、evil_ai.py（easy/normal/hard 原样保留）
                                  ├─ AgentBrain    LLM 智能体（本次新增，§1.3）
                                  └─ HybridBrain   Agent 主决策 + Rule 校验兜底（上线默认形态）
```

新增文件规划（全部在 `backend-python/agents/` 下，不侵入现有 `ai/`）：

| 文件 | 职责 |
|---|---|
| `agents/brain.py` | `Brain` 协议 + `DecisionContext` 数据结构 + `build_brain()` 工厂 |
| `agents/rule_brain.py` | 薄封装，转发到现有 `ai/good_ai.py`、`ai/evil_ai.py` |
| `agents/state_encoder.py` | 棋盘/历史 → 观测 JSON（分正派/反派视角）+ 自然语言局面摘要 + 信念热度图（调 `ai/bayes.py`） |
| `agents/llm_gateway.py` | OpenAI 兼容 API 客户端：模型配置、超时、重试、并发限制、提示词缓存、结构化输出 |
| `agents/agent_brain.py` | 决策循环（§1.3）：推理 CoT → 工具调用 → 结构化动作 → 校验 |
| `agents/prompts.py` | 系统提示词模板（规则 + 角色 + 决策原则 + few-shot 样例），配置化便于热更新 |
| `agents/validator.py` | 动作合法性校验（复用引擎校验逻辑）+ 重试/兜底编排 |
| `agents/tracer.py` | 全量决策轨迹记录（观测、CoT、动作、结果）→ 数据集与评测的原料（§2、§3） |

### 1.3 智能体内部结构与决策流程

单次决策的循环（轻量 ReAct，本游戏每回合是单点决策，不需要多轮工具深挖，CoT + 至多 2 次工具调用足够）：

```
① 感知       StateEncoder 生成观测 O（公开信息 + 己方私有信息 + 信念热度图 + 局面自然语言摘要）
② 推理       LLM 输出 CoT 推理链（可疑度排序 / 对手建模 / 备选方案比较）
③ 可选工具   两枚内置工具，由 LLM 按需调用：
              tool:get_heatmap    → 调 ai/bayes.py 得到嫌疑热度图（正派身份推断）
              tool:list_legal     → 枚举合法动作（正派：可选目标；反派：可用卡 × 可行动反派 × 可选目标）
④ 决策       LLM 以 JSON Schema 结构化输出动作（结构化解码保证格式合法）
⑤ 校验        validator 复跑引擎级合法性检查（与 place_surveillance / play_action_card 同一套规则）
⑥ 执行/兜底   合法 → 执行并记录轨迹；非法 → 携带错误信息重试 1 次 → 仍失败则 RuleBrain 兜底 → 再失败则随机合法动作（对局永不死锁）
```

关键设计点：

- **信念与推理分离**：身份推断的"硬核"（组合空间、约束传播）继续交给 `bayes.py` 以热度图形式提供；LLM 负责**策略层推理**（读局面、定方向、排方案），不做 2024 种组合的算术。这是成本、可靠性与可解释性的平衡点。
- **两个角色两套提示词**：正派智能体强调"排除法 + 站位线索 + 分散监视"；反派智能体强调"击杀收益 − 暴露度 + 声东击西"。两套系统提示词分别维护、分别评测。
- **难度分层**：不再用模型大小区分难度，用**推理预算**区分（CoT 长度上限、工具调用次数、few-shot 数量、temperature）。对应关系建议：easy→RuleBrain easy、normal→RuleBrain normal、hard→AgentBrain（首批上线形态），未来可扩展 `agent-hard`/`agent-normal` 多档。
- **结构化输出**：用 guided decoding（vLLM structured output / outlines / instructor）强制 JSON Schema：`{"cardIndex": int, "actions": [{"villainId": int, "targetId": int, "shape": "九宫格"|"十字"}]}`（反派）或 `{"targets": [int, int, int]}`（正派）。格式错误在生成层就基本消灭。

### 1.4 与现有单人模式代码的对接方式

**接入点只有两个方法**，保持签名与返回结构不变：

```python
# services/game_session.py（改造后）
def __init__(...):
    ...
    self.brain = build_brain(options.get('aiBackend', 'rule'), options)   # 新增

def ai_good_place(self) -> dict:
    ctx = DecisionContext.from_session(self, side='good')       # 状态编码
    targets = self.brain.good_decision(ctx)                     # 大脑决策
    result = self.place_surveillance_action(None, targets)      # 引擎执行（校验不变）
    result['aiTargets'] = targets
    self.tracer.record(ctx, targets, result)                    # 轨迹记录
    return result

def ai_evil_action(self) -> dict:
    ctx = DecisionContext.from_session(self, side='evil')
    action = self.brain.evil_decision(ctx)
    if not action: ...(沿用现有 skip 逻辑)...
    result = self.play_action_card(None, action['cardIndex'], action['actions'])
    result['aiAction'] = action
    self.tracer.record(ctx, action, result)
    return result
```

- `routes/room.py` 的 `CreateRoomBody` 增加 `aiBackend: str = 'rule'` 字段（可选，不传默认走原规则 AI → **对现有玩家零影响**）。
- 异步改造：LLM 调用是 IO 密集，`AgentBrain` 内部用 `asyncio` + 信号量限流（单进程并发决策 ≤ 4），FastAPI 天然适配；`auto_advance` 需改为可等待协程（或由 room_manager 用线程池执行）。
- 现有 `benchmark.py` 跑的是同步纯函数，AgentBrain 需提供同步包装（`asyncio.run`）以便复用评测骨架。

### 1.5 可扩展性设计

| 维度 | 机制 |
|---|---|
| 新玩法/新角色 | `Brain` 协议上增加决策方法即可，`GameSession` 按需扩展 `DecisionContext` 字段 |
| 模型切换 | `llm_gateway` 抽象 OpenAI 兼容协议：云端（DeepSeek/Qwen/GLM/GPT）与本地（vLLM/Ollama）同一接口，配置切换 |
| 策略热更新 | 提示词、few-shot、决策原则全部放 `agents/prompts.py`/配置文件，改策略不重启、不重训 |
| 难度扩展 | 新增难度档 = 新增一组"推理预算参数"（temperature、CoT 上限、few-shot 数），不写代码 |
| 数据闭环 | `tracer` 输出的轨迹即 §2 数据集的原始原料，评测结果自动回灌为标注优先级 |
| 微调/RL | AgentBrain 内部再留一个 `ModelBackend` 接口（API 调用 vs 本地部署模型推理），SFT/RL 后的模型替换后端即可，上层零改动 |

### 1.6 技术选型建议

| 决策点 | 建议 | 理由 |
|---|---|---|
| 云端模型（起步） | DeepSeek-V3 或 Qwen3-Max（OpenAI 兼容 API） | 推理任务中文 CoT 质量好、单价低；深度思考类（R1）按需用于"困难档" |
| 本地模型（规模化/隐私） | Qwen3-8B / Qwen2.5-14B + vLLM（支持 guided decoding） | 单机可跑、可控延迟、支持后续 LoRA 微调产物替换 |
| Agent 框架 | **不引入 LangChain/LangGraph**，手写轻量循环（约 200 行） | 决策点少、动作空间小，框架引入的抽象收益低、依赖与调试成本高；需要多步规划时再评估 |
| 结构化输出 | vLLM structured output（本地）/ outlines 或 instructor（云端 fallback：schema 提示 + 解析重试） | 把"动作格式非法"压缩到最低，直接服务 L1 指标 |
| 微调 | Unsloth + LoRA（SFT 蒸馏人类 CoT）；GRPO/DPO 用 verl | 与 Qwen 系生态兼容，单卡可训 |
| 评测 | 扩展现有 `benchmark.py`，固定种子、输出 JSON 报告 | 直接复用口径，新旧大脑同台对比 |

---

## 二、数据集标注方案

### 2.1 样本采集来源（按价值排序）

| 来源 | 采集方式 | 价值 | 用途 |
|---|---|---|---|
| **S1 人类出声思考** | 用户游玩时按 §附录A 模板记录每个决策点（局面截图/ID + 思维链 + 选择） | ⭐⭐⭐ 最高 | CoT SFT 核心数据、few-shot 样例 |
| **S2 人类复盘点评** | 用户赛后对 AI 决策回放逐条点评（同意/反对 + 理由） | ⭐⭐⭐ 高 | DPO 偏好对、原则抽取 |
| **S3 机器自对弈 + 人工修正** | 现有三档 AI 互打（`benchmark.py` 固定种子批量产出），人工筛选高质量对局并修正错误决策 | ⭐⭐ 中 | 预训练规模补充、验证集 |
| **S4 规则"参考最优"弱标注** | hard 档搜索/极小化结果作为该决策点的参考动作（不当作 ground truth） | ⭐ 弱监督 | 一致性参考、LLM 决策的对比基线 |
| **S5 上线后真实对局** | `tracer` 自动记录线上对局 + 玩家胜负反馈 | ⭐⭐ 持续增量 | 数据飞轮、长尾局面覆盖 |

### 2.2 单条样本字段结构（Schema）

以"正派第 3 回合监视决策"为例：

```json
{
  "sample_id": "good-20260901-0007",
  "source": "S1_human_thinkaloud",            // S1~S5，见 §2.1
  "game_id": "20260901-9f3c-0007",
  "game_meta": {
    "seed": 42,
    "villains": [203, 401, 504],               // 仅供事后标注与评测，不进入智能体观测
    "ai_difficulty_opponent": "hard",
    "result": "evil_win", "total_rounds": 6
  },
  "decision_point": {
    "side": "good",
    "round": 3, "phase": "placement",
    "public_state": {                          // 双方可见信息（智能体唯一观测来源）
      "board": [{"id": 201, "row": 0, "col": 0, "status": "alive",
                 "hasDeathMarker": false, "surveillanceActive": false}],
      "history": {"round1": {"deathMarkers": [{"targetId": 302, "shape": "十字"}],
                              "cardIndex": 1},
                  "round2": {"surveillance": [301, 402, 505], "deathMarkers": []}},
      "remaining_cards": [0, 2, 3, 4]
    },
    "belief_state": {                          // 可选：正派视角贝叶斯热度图（bayes.py 输出）
      "heatmap": {"402": 0.31, "203": 0.27, "504": 0.18, "305": 0.09},
      "alive_candidates": 22
    },
    "legal_actions": {
      "type": "choose_3_surveillance",
      "candidates": [201, 203, 204, 301, 305, 402, 405, 502, 504]
    }
  },
  "human_label": {
    "chosen_action": {"targets": [203, 402, 504]},
    "thought_process": [                        // 人类思维链（CoT 蒸馏原料）
      "302 被十字标记，凶手必然在它的同行同列，402 与 504 都在范围内，嫌疑上升",
      "排除 201、204：上回合已被排除或监视过无信息",
      "305 虽然热度低，但如果反派声东击西，分散一个监视位更稳"
    ],
    "principles_used": ["exclude_killed", "region_constraint", "spread_risk"],
    "confidence": 0.8,
    "annotator": "player_zhang", "annotated_at": "2026-09-01T17:00:00+08:00"
  },
  "outcome": {                                 // 事后回填（对局结束/回放后）
    "chosen_action_hit": 2,
    "win": false,
    "counterfactual_note": "若选 305 而非 504，下回合 504 的十字标记可再排除一批人"
  },
  "quality": {"reviewed": true, "reviewer": "player_zhang",
              "issues": [], "grade": "A"}
}
```

反派侧决策点的 `legal_actions` 与 `chosen_action` 结构相应变为：可选卡 ×（可行动反派 × 可选目标 × 形状）的组合枚举，`chosen_action = {"cardIndex": 2, "actions": [{"villainId": 504, "targetId": 203, "shape": "十字"}]}`。

**字段设计要点**：
- `public_state` 与 `belief_state` 分离——前者是信息边界内的原始事实，后者是可选的推理辅助；两者分开存储，保证训练/评测时可以独立开关（验证"不给热度图时智能体是否退步"）。
- `human_label.thought_process` 是**唯一标注硬劳动**，其余字段全部可由录制系统自动生成。
- `outcome` 与 `quality` 允许事后回填，标注不需要在对局进行中完成。

### 2.3 标注规范（状态描述 / 可选动作 / 最优动作 / 理由）

1. **状态描述规范**：标注不要求抄写局面（系统自动生成），但 `thought_process` 必须**引用可核实的事实**（角色 ID、标记位置、回合号），禁止只写"感觉某人是反派"。每条思维链 2~6 句，覆盖"看到什么 → 推断什么 → 比较什么 → 决定什么"四段。
2. **可选动作规范**：由引擎枚举生成，标注时**不允许添加或删除**任何合法动作；若发现引擎漏枚举，走 bug 通道而非改标注。
3. **最优动作判定标准**（优先级从高到低）：
   - a) 规则共识：违背排除法的动作（监视已被排除/已死者）一律判错，一票否决；
   - b) 长期模拟：以该动作为节点，用 hard 档对手双方模拟 100 局，比较胜率/命中率；
   - c) 人类共识：多人标注取多数；
   - a 为硬标准，b/c 为软标准，冲突时 a > b > c。
4. **理由分级**：
   - **L1 原则级**（"排除已被杀者"）：映射到 `principles_used` 枚举，便于统计与原则抽取；
   - **L2 推理链级**（多步因果）：写入 `thought_process`，是 CoT 蒸馏的直接原料。
5. **多最优处理**：当多个动作在 b) 标准下表现无显著差异（模拟胜率差 < 2%）时，标记为"等价多解"，`chosen_action` 之外的等价解写入 `quality.issues` 备注，训练时作为"可接受集合"而非唯一答案。

### 2.4 一致性与质量保证

| 机制 | 做法 |
|---|---|
| 标注指南 | 《标注指南 v1》：标注范围、四段式思维链要求、最优判定三标准、30 个正反例 |
| 金标准集 | 从 S3 中挑 20 条规则共识样本，由用户标注后作为**答案已知的校准集** |
| 双标注 + 仲裁 | 每批样本 10% 双人标注（用户 + AI 标注员），动作不一致时以金标准+模拟裁决 |
| 一致性指标 | 动作一致率 ≥ 90%；思维链要点重合率 ≥ 80%；每次标注后计算，低于阈值回炉 |
| 抽检 | 每周随机抽 10% 已标注样本复检，`grade` 为 B/C 的强制二次审核 |
| 版本管理 | 数据集带 schema 版本号与标注指南版本号，规则变更（如 §0.2 的规则层调整）时冻结旧集、开新集 |

---

## 三、智能体分阶段能力评价标准

### 3.0 通用口径（贯穿四阶段）

- **评测基建**：扩展 `benchmark.py` → `agents/eval/`，固定 100 个种子局面集（每局对局 ID 可复现），输出 JSON 报告（胜率、命中率、合法率、延迟、成本、决策分布）。
- **指标口径统一**：
  - **命中率** = 正派每次监视决策命中的真实反派数均值（0~3，沿用现有口径，是**首要指标**）；
  - **胜率** = 正派胜局占比（2026-09-01 范围规则修正后，正派 normal/hard 胜率恢复至 83~90%，**胜率升级为有效首要指标之一**）；
  - **合法率** = 通过引擎校验的动作占比（智能体的底线指标）；
  - **利用率**（反派）= 实际放置标记数 / 卡面允许数；
  - **暴露度**（反派）= 对手监视命中数 / 己方行动数（越低越隐蔽）；
  - **决策延迟/成本** = 单决策耗时（ms）与 API 费用（元/局）。
- **基线对照**：RuleBrain 三档在全部指标上的数值即每一阶段的"及格线参照"。
- **防泄漏**：训练集与评测集种子集合**硬隔离**，评测集样本不进任何训练/提示样例。

### 3.1 阶段划分总览

```
L1 基础操作 ──► L2 策略理解 ──► L3 复杂决策 ──► L4 泛化
（会下棋）      （会推理）       （会对抗）       （举一反三）
```

**门禁制**：每阶段评测达标才进入下一阶段；L3 通过才允许替换 hard 档上线；L4 通过才开放"智能体专属难度档"。

### 3.2 L1 基础操作（能合法完成对局）

| 项目 | 标准 |
|---|---|
| 评价指标 | ① 动作合法率 ② JSON 格式正确率 ③ 决策延迟 ④ 边界场景覆盖率 |
| 测试场景 | 100 局全流程对局（正派侧 + 反派侧各 100）；边界场景集：无可用行动卡、全部反派被监视、无可标记目标、第 6 回合跳反派行动、仅剩 3 个候选等 |
| 通过标准 | 合法率 = 100%（含重试后）；格式正确率 ≥ 99%；延迟 P95 ≤ 3s（云端）且不导致对局卡死；边界场景对局全部正常走完 |

**说明**：本阶段用 RuleBrain 兜底是允许的，但兜底率要单独记录（≥ 5% 视为异常，说明提示词/校验链有问题，回炉）。

### 3.3 L2 策略理解（掌握规则内推理）

| 项目 | 标准 |
|---|---|
| 评价指标 | ① 正派命中率（vs easy 反派）② 基础错误率 ③ 反派行动利用率 ④ 排除法合规率 |
| 测试场景 | 正派 Agent vs easy 反派 100 局；反派 Agent vs easy 正派 100 局；注入 50 个"含明显排除线索"的定制局面（如被杀者出现于候选名单） |
| 通过标准 | 正派命中率 ≥ 1.70（参照：现有 normal 档 1.72~1.80）；基础错误率 = 0（监视已死者/已排除者/重复监视，含**越范围动作判定**）；反派利用率 = 100%；排除法合规率 = 100% |

**说明**：本阶段考察"懂了规则"，即智能体能稳定复现现有 normal 档水平，并展示可解释的推理链（CoT 中能说出排除依据）。

### 3.4 L3 复杂决策（多回合规划与对抗）

| 项目 | 标准 |
|---|---|
| 评价指标 | ① 正派 vs hard 反派命中率与胜率 ② 残局模拟胜率 ③ 行动卡选择质量（反派侧指标延后至反派智能体倒推设计时制定） |
| 测试场景 | 正派 Agent vs hard 反派 100 局；残局集：从第 4/5 回合截断的 50 个局面续打至终局；"信息稀释"专项局（反派选择范围与其他高嫌疑角色重叠多的目标） |
| 通过标准 | 正派 vs hard 命中率 ≥ 1.70 且胜率 ≥ 80%（参照：现有 hard 档 1.73 / 83~90%）；残局模拟胜率 ≥ RuleBrain hard 水平；信息稀释专项局中能正确运用 P3/P4 硬计数反制（不盲信范围重叠） |

**说明**：本阶段是对抗能力的分水岭。通过后智能体即可替换 hard 档试上线，与 RuleBrain hard 做 A/B。

### 3.5 L4 泛化（未见局面与规则变体）

| 项目 | 标准 |
|---|---|
| 评价指标 | ① 新种子性能保持率 ② 规则变体合法率与适应度 ③ 干扰鲁棒性 ④ 人类相似度 |
| 测试场景 | 全新 100 种子（与训练/评测前集隔离）；规则变体：4 反派局、监视跨回合累积局、新行动卡池（改参数不改代码）；对抗干扰：极端局面（首回合全员高嫌疑）、提示注入测试（局面描述中夹带误导性语句）；人类相似度：决策分布与 S1 人类记录对比 |
| 通过标准 | 新种子各项指标较 L3 基准下降 ≤ 5%；变体局合法率 = 100% 且命中率不低于 RuleBrain hard 在变体下的表现；干扰下决策不劣化（与无干扰对照差异 ≤ 5%）；人类相似度：top3 选择重合率 ≥ 50%，且不出现人类不会犯的基础错误 |

**说明**：L4 是"智能体档"的开放门槛，也是把 §2 数据飞轮（S5 线上数据）纳入常态更新的起点。

---

## 四、人类经验的转化路径

> 用户将提供：人类玩家游玩游戏的解题思维与决策过程。本节说明它们如何变成智能体的先验知识、奖励设计与训练约束。**采集模板见附录 A**（建议每局 5~8 条决策点记录 + 1 份局后复盘）。

### 4.1 四条转化路径总览

```
人类经验
  ├─ ① 原则抽取 ──────────► 系统提示词"决策原则"段 + 硬约束清单（先验知识，即时生效）
  ├─ ② CoT 蒸馏 ──────────► SFT 训练集（few-shot 样例 → LoRA 微调，中期生效）
  ├─ ③ 偏好/奖励注入 ─────► DPO 偏好对 + GRPO 奖励塑形（长期生效）
  └─ ④ 违规样本对照 ──────► 训练约束与评测负例库（全程生效）
```

### 4.2 路径①：先验知识（即时、零训练成本）

- **原则抽取**：从人类 `thought_process` 与 `principles_used` 中归纳高频原则，如"排除已被杀/已排除者""十字标记反推凶手可行域""监视分散防声东击西""反派优先击杀边缘低暴露角色""优先用完行动卡次数"。每条原则形成：名称 + 触发条件 + 动作建议 + 反例，写入 `agents/prompts.py` 的决策原则段。
- **先验注入**：现有 `bayes.py` 的几何站位似然强度 `ALPHA=2.0` 是"站位线索"的数值先验——用人类数据校准它（统计人类实际依据站位线索改判的比例，反推 ALPHA），让"人类直觉"变成可调参数，智能体通过热度图间接继承。
- **few-shot 注入**：从 S1 数据精选 3~5 条高质量"局面 → 思维链 → 动作"样本（每阵营各一套）直接放进提示词，作为风格与推理深度的锚点。

### 4.3 路径②：CoT 蒸馏（中期，SFT）

- 将 `human_label.thought_process` 作为**答案的理由部分**，`public_state + belief_state` 作为输入，`chosen_action` 作为输出，构造 SFT 样本（指令微调格式：系统提示词 + 局面 + "请分析并决策" → 思维链 + JSON 动作）。
- 数据规模阶梯：few-shot（≥ 20 条精选）→ LoRA SFT（≥ 300~500 条，覆盖正反派、各回合、各局面类型）→ 全量微调（≥ 2000 条）。
- **人类 CoT 优先于机器 CoT**：S3 自对弈也能生成思维链（让现有 AI 复述评分依据），但仅作冷启动与数据增强，训练权重低于 S1。

### 4.4 路径③：奖励设计与偏好（长期，RL/DPO）

- **主奖励（环境真实信号）**：胜 = +1；分步 shaping——正派每命中一个反派 +0.3（结算时按真实身份计，**只在训练时可见**，推理时严格隔离）；反派每标记一个目标 +0.2、被监视惩罚 −0.3。
- **人类原则辅助奖励**：违反人类高频原则的动作给予小额负奖励（如正派监视已排除者 −0.5），把"解题思维"变成可微的引导信号。
- **DPO 偏好对**：同一决策点，"人类选择 vs 智能体落选方案"构成偏好对（S2 复盘点评亦可构造：被人类反对的动作为 rejected）。偏好对优先用 GRPO（verl + 环境对局结果）做最终强化。
- **诚实性约束**：奖励只奖励"合法且信息边界内"的行为——智能体通过偷看答案获得的高奖励在架构上不可能发生（观测层根本不传身份信息），这是硬约束而非软奖励。

### 4.5 路径④：训练约束与负例库

- **硬约束（架构层，非训练层）**：动作合法性校验 + 信息边界过滤，见 §1.1、§1.3，人类经验不参与、也不能豁免。
- **负例库**：S2 中被人类明确反对的 AI 决策、以及 L2 评测中的基础错误样本，集中为"负例集"，用于 SFT 数据配平（正负比 ≈ 3:1）与评测回归。
- **风格软约束**：思维链要求"引用可核实事实""四段式结构"（§2.3），与人类表达习惯对齐，也保证可解释性。

### 4.6 用户提供材料的最低可行清单

| 材料 | 数量建议 | 直接用途 |
|---|---|---|
| 出声思考决策点记录（附录 A 模板） | 30~50 条 | few-shot + 原则抽取（路径①②启动） |
| 局后复盘（每局 3~5 句 + 关键失误点） | 10 局 | 原则抽取 + 负例库 |
| 对 AI 回放点评（同意/反对+理由） | 50 条 | DPO 偏好对 + 负例库 |
| 个人"常用原则"清单（自由书写 10~20 条） | 1 份 | 提示词决策原则段初稿 |

---

## 五、实施路线图与优先级

| 阶段 | 内容 | 产出物 | 优先级 |
|---|---|---|---|
| **P0 架构落地**（1~2 周） | Brain 抽象 + StateEncoder + LLM 网关 + Validator + Tracer；云端模型跑通全流程；RuleBrain 全量兜底 | 可对局的 AgentBrain（few-shot 版）+ L1 评测全绿 | 🔴 最高（风险收敛点，见 §6-1） |
| **P1 数据管线**（与 P0 并行，2~3 周） | 对局/决策录制、标注指南 v1、金标准集、用户提供首批 S1/S2 材料、标注工具（表格/脚本即可） | 300+ 条带标注决策样本 + 标注一致性报告 | 🔴 高（决定 P2 质量） |
| **P2 能力提升**（3~4 周） | 原则/few-shot 注入（P0 完成后立即做）→ 满 500 条后 LoRA SFT → L2/L3 评测 → 换 hard 档 A/B 试上线 | 通过 L3 的 SFT 版智能体 + 评测报告 | 🟡 中 |
| **P3 强化与泛化**（4~6 周） | GRPO/DPO、L4 评测、本地模型部署与成本优化（提示缓存/模型替换）、数据飞轮（S5 线上回灌） | 通过 L4 的"智能体档" + 成本报告 | 🟢 中低 |
| **持续运营** | 新对局 → 自动录制 → 抽样标注 → 定期重训 → 评测回归（每次上线前必须跑完整四阶段回归） | 月度模型迭代 | 🔵 常态化 |

**依赖关系**：P0 是 P2/P3 的硬前提；P1 与 P0 并行可节省 2 周；P2 的 SFT 依赖 P1 数据量，不足则退化为"纯 few-shot + 原则注入"（也能过 L2，是保底路径）。

---

## 六、风险点与对策

| # | 风险 | 影响 | 对策 |
|---|---|---|---|
| 1 | **LLM 幻觉/非法动作导致对局卡死** | 高 | 结构化解码 + 引擎级校验 + 重试 1 次 + RuleBrain 兜底 + 随机合法动作兜底（§1.3 五级防线）；兜底率超 5% 自动告警 |
| 2 | **信息泄漏/作弊**（智能体偷看反派身份） | 高（违背游戏公平与"不作弊"原则） | 观测层只从 `get_public_board(viewer_role)` + 公开历史构建；加"完美正派"审计测试（向观测注入假信息，验证智能体决策变化与信息一致性）；代码评审红线 |
| 3 | **成本与延迟失控**（每局 10 个决策点 × API 调用） | 中高 | 起步用低价模型；提示词前缀缓存；热度图工具化减少 LLM 算术；P3 迁本地 vLLM；单决策 P95 ≤ 3s 纳入 L1 硬指标 |
| 4 | **难度失控/体验不一致** | 中 | 难度 = 推理预算参数而非模型大小；上线前跑三档回归（RuleBrain 各档基线对比）；temperature/seed 可复现 |
| 5 | **评测泄漏**（训练集包含评测局面） | 中 | 种子集硬隔离 + 样本 provenance 字段；评测集只在发布报告时生成 |
| 6 | **人类数据稀缺/质量不稳** | 中 | 先跑通 P0 用 S3 自对弈冷启动；金标准 + 双标注 + 抽检（§2.4）；数据不足时走"纯 few-shot"保底路径 |
| 7 | **非确定性难排查** | 低中 | Tracer 全量记录（观测、CoT、动作、模型版本、温度）；同 seed 可复现重放；A/B 对比固定配置 |
| 8 | **平衡反转**（2026-09-01 范围规则修正后正派胜率 83~90%，反派偏弱） | 中（已出现） | 正派侧评测指标现以命中率+胜率并用（§3.0 已更新）；反派侧策略与平衡按约定**延后到正派智能体完成后倒推设计**（合法范围内信息稀释、行动卡节奏、残局压制）；数据集与评测集随规则版本管理（schema 版本化，§2.4） |
| 9 | **依赖锁定**（某 LLM 厂商/框架变更） | 低 | OpenAI 兼容网关 + 模型后端抽象；框架零依赖；提示词与数据完全自持 |

---

## 附录 A：人类决策点记录模板（交付用户的采集表单）

> 每局按此模板记录 5~8 个关键决策点（不必每个回合都记，挑"有想法"的时刻）。局面信息由录制工具自动带出，你只需填"思考"部分。

```text
【决策点卡片】
- 对局 ID / 回合 / 阶段 / 阵营：（自动带出）
- 当前局面一句话：（自动带出：如"第3回合，302 上回合被十字标记，我手上还剩4张卡"）

1. 我重点看到了什么？（关键事实：谁死了、谁被监视、标记形状与位置）
2. 我推断什么？（可疑度排序：最怀疑谁、为什么、次怀疑谁）
3. 我考虑过哪些选项？（至少 2 个备选，各写一句利弊）
4. 我最终选择 & 理由：（对应 chosen_action）
5. 事后回顾：（结果揭晓后回填：猜对了吗？如果重来会怎么做？）
```

```text
【局后复盘】（每局 3~5 句）
- 本局我赢/输的关键转折点是什么？
- 哪一步决策回头看不应该那样做？正确的是什么？
- 对手（AI/人）哪个动作最出乎我意料？
```

```text
【个人常用原则清单】（一次性填写，10~20 条，自由书写）
例：
1. 先排除已被标记/被杀的角色，绝不浪费监视位。
2. 十字标记比九宫格信息量大，凶手大概率在十字线上。
3. 反派会尽量避免站进自己标记的范围内，所以"标记范围外的高活跃角色"反而可疑。
4. 第 5、6 回合要冒险集中监视，不能再分散。
...
```

**交付物形态建议**：Markdown/表格文件即可；`thought_process` 字段按四段式（看到 → 推断 → 比较 → 决定）整理后，即可直接入库成为 S1 样本。

---

*本方案与现有 `ai_upgrade_report.md` 互为表里：后者定义了"规则 AI 能走多远"，本方案定义了"智能体如何在此基础上再进一步，以及如何被度量"。*

---

## 附录 B：2026-09-01 规则修正补遗（范围内标记强制执行）

### B.1 发现

用户指出"反派不可以越过标记的范围进行标记"。核查发现三层实现不一致：

| 层 | 原状态 | 处理 |
|---|---|---|
| 前端（真人） | `GameBoard.tsx` 已强制"只能放在范围内" | 无需改动 |
| 引擎 | `place_death_marker` **无范围校验**（API 可绕过） | ✅ 已补硬校验 |
| 反派 AI | 评分函数把"站出范围"奖励为 exposure=0，normal/hard 系统性越范围标记 | ✅ 已收窄搜索空间 |

2026-08-16 报告中的"声东击西/规避暴露"（正派命中率 0.73→0.41~0.56）本质是 AI 相对真人的不对称优势。旧基线全部作废。

### B.2 代码改动清单

1. `services/game_engine.py`：`place_death_marker` 增加范围校验（反派 ∈ 标记影响范围且 ≠ 目标）。
2. `ai/evil_ai.py`：`_score_triplet` 越范围组合返回 `-inf`；`_greedy_selection` 跳过非法组合；`_evil_easy` 目标过滤进范围内。暴露度恒生效（范围内必然暴露）。
3. `ai/bayes.py`：新增 `GEOMETRY_HARD` 开关（`ai/config.py`，默认 `True`）——组合必须满足"每个标记都能指派一名范围内活跃反派"（一一指派），否则剔除；软似然加权保留。
4. `smoke_test.py`：场景 2 改为按新规则生成合法动作；全部冒烟通过。
5. `services/game_session.py`（2026-09-02 追加）：信息边界修复——`_sanitize_reveal` + `get_round_records(viewer_role)` 对非反派视角剥离 deathMarkers 的 `villainId`（原接口层会把行动者身份下发给正派客户端，前端虽不渲染但属泄漏）。已用泄漏检查脚本验证。
6. 2026-09-02 追加：满员行动硬规则（`play_action_card` 恰好 max_actions + `ai/evil_ai.py` `find_full_assignment` 回溯求解）、禁标同伙（`place_death_marker` role 校验）、403 复活变体（`revive403` 全链路：引擎/AI/会话/路由/前端/基准）、反派 AI 对抗评分（倒推设计落地，见 `villain-ai-reverse-design.md` §5.1 验收数据）。
7. 2026-09-02 追加（P0 完成）：`agents/` 包补齐——`state_encoder.py`（信息边界过滤观测 + 信念热度图 + 动作空间枚举）、`prompts.py`（P1~P8 / R1~R6 系统提示词，策略热更新中心）、`llm_gateway.py`（OpenAI 兼容网关，纯标准库，环境变量配置）、`validator.py`（引擎同口径动作校验）、`agent_brain.py`（编码→LLM→解析→校验→重试→回退的决策管线）、`demo.py`（智能体 vs 规则 AI 对战验收脚本）；`brain.py` 注册 'agent'/'hybrid' 后端。**至此 P0 架构全部落地**：LLM 未配置时自动回退 RuleBrain，对局永不卡死。
8. 2026-09-02 追加（P1 启动）：`agents/eval.py`（L1 评测 harness：合法率/格式正确率/回退率/延迟 + 7 项边界场景检查）、`agents/recorder.py`（决策点录制器：对局 → §2.2 数据集样本 JSON）、`human-knowledge/annotation-guide.md`（标注指南 v1）；`AgentBrain` 增加 parse_errors/call_failures 计数器。同时修复**游戏循环保真度 bug**：benchmark/demo/eval/recorder 四处的对局循环此前在第 6 回合虚构了反派行动阶段（规则上第 6 回合正派行动后直接结算），已全部改为跳过——录制器样本数从 11 条/局修正为 10 条/局，基准胜率不受影响（仅决策耗时统计口径变准）。

### B.3 对智能体化的影响

- **动作校验器必须包含范围校验**（列入 L1"合法率"规则项），防止智能体继承越范围作弊行为；
- 数据集标注规范中"越范围动作"一票否决（§2.3 硬标准 a）；
- §3 评测基线以新基准为准（见 B.4）；
- 反派智能体的策略设计**延后到正派智能体完成后**，以正派解题框架（`human-knowledge/good-principles.md` P1~P6）倒推设计——即"先定解题思路，再倒推出题思路"。

### B.4 新基准（2026-09-01 重测，每格 100 局）

【正派胜率】正派\反派：easy → easy 0.0% / normal 2.0% / hard 0.0%；normal → 85.0% / 88.0% / 86.0%；hard → 89.0% / 83.0% / 90.0%。

【正派监视命中率（每次决策平均命中反派数 0~3）】easy → 0.51 / 0.54 / 0.53；normal → 1.80 / 1.72 / 1.76；hard → 1.75 / 1.74 / 1.73。

【单局决策耗时 ms】easy → 0.1 / 19.5 / 67.7；normal → 20.7 / 34.9 / 78.1；hard → 127.0 / 145.7 / 181.3。

Sanity check：完美正派 20/20 全胜（游戏循环在新规则下正确）。

**平衡结论**：范围硬约束使正派推理精度暴涨（normal/hard 命中率 1.7~1.8，旧基线 0.56~0.73），胜率从 ≈0% 翻转为 83~90%。正派（normal/hard）现为强势方；反派侧策略与平衡调整**按约定延后**——待正派智能体完成后倒推设计反派（合法范围内的信息稀释、行动卡节奏、残局压制等）。§3 各阶段阈值已按新基线更新。
