# 反派 AI 倒推设计方案（基于正派解题框架 P1~P8）

> 版本：v1.0 ｜ 2026-09-02 ｜ 前提：正派框架已完整（`human-knowledge/good-principles.md` P1~P8 + 样例 S1~S3）
> 方法论（用户定调）：**正派 AI 完成后，倒推设计反派 AI**——先定解题思路，再倒推出题思路。

---

## 0. 倒推的逻辑起点

**反派每放一个死亡标记，对自己都是纯信息损失**：

| 标记的后果 | 影响 |
|---|---|
| 暴露站位范围（P1：凶手必在范围内） | 直接出卖自己 |
| 帮正派排除目标（被杀者不可能是反派） | 缩小正派搜索空间 |
| 缩小无辜者藏匿池（被标记者不可再被监视，候选池缩水） | 让反派更难藏身 |

反派赢在"活过第 6 回合"，与击杀数量**无关**；而规则又强制其每回合至少行动。因此：

> **反派 AI 的唯一课题 = 被迫行动下的泄漏最小化。**

这与旧反派 AI 的设计哲学（"击杀收益最大化"）完全不同——旧哲学默认了"杀人有利"，而实际上杀人对反派零收益。这是本次倒推设计要纠正的第一性错误。

---

## 1. 三个规则前提（需设计方拍板）

| # | 规则 | 状态 | 理由 |
|---|---|---|---|
| 1.1 | 反派必须站进标记影响范围 | ✅ 硬规则（2026-09-01 已实施） | 用户拍板方案 A |
| 1.2 | **反派必须满员行动**：放置标记数 k = min(卡行动次数 c, 可行动反派数 a) | ✅ **硬规则（2026-09-02 已拍板并实施）** | "能动时必须动"。引擎 `play_action_card` 强制恰好 max_actions；反派 AI 用 `find_full_assignment` 回溯求解保证满员指派；跳过判定按"是否存在满员方案"执行 |
| 1.3 | **禁止标记己方反派** | ✅ **硬规则（2026-09-02 已拍板并实施）** | 引擎 `place_death_marker` 校验 target.role != 'evil'，保护"被杀者不可能是反派"公理 |

**实施记录**：引擎（`place_death_marker` 禁同伙 + `play_action_card` 满员校验）、`ai/evil_ai.py`（`find_full_assignment` 回溯求解器 + easy 档改用随机化满员指派 + normal/hard 不可行卡跳过）、`ai/search.py`（目标过滤改状态驱动）、会话层跳过判定（按满员可行性）、前端本就强制满员提交（`submitEvilActions` 校验 length === maxActions），冒烟测试场景 2 改为满员指派。

---

## 7. 403 复活变体（正派困难档，2026-09-02 新增）

**机制**：`revive403=True` 时，403 复活——正常存活、可被标记、可入反派池（候选从 24 人变 25 人）。

- **对正派**：候选空间 +1（C(24,3)=2024 → C(25,3)=2300），且 403 可作为反派或普通角色参与全部推理 → 正派任务显著变难 → 用于正派困难档；
- **对反派**：藏匿池 +1、可标记目标 +1 → 反派任务变易 → 该变体下反派难度自然降档（配反派 easy/normal 档）。

**已实施**：引擎（`create_initial_board(villains, revive403)` / `draw_villains(revive403)` / 监视候选按状态）、`bayes.py`（候选集按状态）、`evil_ai`/`search.py`（403 过滤改状态驱动）、`game_session`（`revive403` 选项 + 状态透出）、`routes/room.py`（`revive403` 字段）、前端 GameBoard 三处 403 过滤改状态驱动（复活时 403 自动可交互）、`benchmark.py --revive`、冒烟测试场景 4。

**难度映射提案（待确认）**：

| 单人模式难度 | revive403 | AI 反派档（玩家为正派时） |
|---|---|---|
| 简单 | off | easy |
| 普通 | off | normal |
| 困难 | **on** | normal（复活模式反派天然占优，无需 hard 档） |

**实测结论（2026-09-02，每格 100 局）**：复活变体对正派胜率**几乎无影响**——普通矩阵 normal/hard 正派胜率 86~93%，复活矩阵 86~93%，命中率同样持平（1.70~1.81）。原因：正派硬约束推理（被杀者排除 + k=min(c,a) + 范围内指派）的收敛速度对候选池大小不敏感，+1 候选只增加 log₂(2300/2024)≈0.18 bit 的不确定性。**正派困难档真正的难度杠杆不在规则变体，而在反派 AI 的对抗策略质量（R1~R6 尚未实施）**。复活变体保留为难度调味与可选玩法，实施 §5 的对抗评分函数才是平衡正派胜率（目标回落 40~60%）的主抓手。

---

## 2. 倒推反制原则库（P → R 对照）

| 正派原则 | 反派反制 | 内容 |
|---|---|---|
| P1 范围划定罪 | **R1 稀释** | 每个标记选"范围内无辜者多、其他反派少"的目标，让正派的嫌疑范围尽量拥挤、尽量不含同伙 |
| P2 九宫格优先 | R1 延伸 | 优先消耗十字卡（范围长条化、难被 3 个监视点踩住）；九宫格卡留给精确干扰 |
| P3 行动次数反推 | **R2 卡序调度** | 大卡（3 动）留到"有反派被监视（a 小）"的回合打，容量被浪费 = 少泄漏；小卡在满员回合打。最小化全局标记总量 |
| P4 放钓实验 | R2 + R3 协同 | 放钓的 k 反馈无法造假（满员规则保证），但**由谁作案、标在哪**可控——让被"释放验证"的反派保持低调 |
| P5 跨回合叠加 | **R3 作案人轮换与时序错位** | 同一反派避免连续标记形成小交集；三人轮换作案，个人范围交集保持大而拥挤 |
| P6 范围内稀释 | R1（同源） | 规则修正后"声东击西"唯一合法形态就是 R1 |
| P7 残局放钓位 | **R4 残局模糊** | 第 5 回合选择"让 k 反馈的解释空间最大"的方案——标记范围与正派候选集的关系尽可能模糊 |
| P8 全局复盘一致性 | **R5 叙事模糊** | 让公开历史能被**尽可能多的三人组**解释，压低真实三人组的后验概率（后验熵最大化） |
| —（旧 AI 已有） | **R6 防误杀** | 不标记正派高度怀疑的无辜者（purge 惩罚，避免帮正派排除错误假设） |

---

## 3. 目标函数（工程核心）

反派 AI 的核心组件 = **正派推断器的对抗模拟**（复用 `ai/bayes.py`，严格信息边界）：

```
evil_score(plan):
    # 1. 模拟：执行 plan 后正派视角的后验热度图
    heat = build_inference(board_after, history + plan观测).marginal()
    # 2. 三部分得分（反派知道自己的真实身份，模拟只用公开信息）
    exposure      = Σ heat[v]  for v in 真反派        # 最小化：真反派暴露度
    innocent_heat = Σ heat[i]  for i in 无辜者        # 最大化：让正派怀疑错人
    purge_penalty = 标记中"正派高嫌疑无辜者"的数量     # 最小化：别帮正派排除
    score = -EXPOSURE_W * exposure + DECOY_W * innocent_heat - PURGE_W * purge_penalty
```

即：**最小化正派对真反派的集中度**。权重入 `ai/config.py`（新增 `EVIL_ADVERSARIAL_WEIGHTS`）。

---

## 4. 难度分层（沿用"搜索深度分层"哲学）

| 档位 | 策略 |
|---|---|
| easy | 范围内随机（同现状，泄漏自然大 → 最弱） |
| normal | 贪心对抗：单步最大化 `evil_score`（不前瞻） |
| hard | 前瞻一层：枚举候选方案 → 对每个方案模拟**正派 hard 档的最优响应**（对称复用 `good_ai` 的一层极小化结构）→ 选"正派响应后我方暴露最小"的方案 |

搜索结构保持现有"卡 × 反派指派 × 范围内目标"的全排列框架（`ai/search.py` 可复用，评分函数替换）。

---

## 5. 实施步骤与验收

1. ✅ **规则前提**（§1）：满员行动、禁标同伙已定为硬规则并实施（2026-09-02）；
2. ✅ **对抗评分实施**（2026-09-02）：`ai/bayes.py` 抽出纯函数 `_apply_observation` / `_marginal_from`（正派推断与反派对抗模拟共用，保证数学一致）；`ai/evil_ai.py` 新增 `_plan_exposure`（模拟正派后验 → 真反派暴露度之和）、`_proxy_score`（R1/R6 可解释代理）、`_greedy_adversarial_plan`（普通档）、`_beam_adversarial_plan`（困难档 beam 宽 3）、`_evil_adversarial`（主入口）；`ai/config.py` 新增 `EVIL_ADVERSARIAL_WEIGHTS`；
3. ✅ **基准重跑**（每格 100 局，普通 + 复活双矩阵）：**验收达成**；
4. ⏸ 权重调参（可选微调）；
5. ⏸ 出《反派倒推设计验收报告》（本基准数据即验收依据）。

### 5.1 验收数据（2026-09-02，每格 100 局）

**普通矩阵【正派胜率】**：easy 正派 0~1%；normal 正派 vs easy/normal/hard 反派 = 89% / **62%** / **50%**；hard 正派 = 91% / 69% / **48%**。

**普通矩阵【正派命中率】**：normal 正派 vs easy/normal/hard = 1.79 / **1.01** / **0.73**；hard 正派 = 1.74 / 1.02 / 0.68（对抗前同格 ≈1.72~1.81，稀释效果显著）。

**复活矩阵【正派胜率】**：normal 正派 vs easy/normal/hard = 84% / **52%** / **44%**；hard 正派 = 86% / 60% / **38%**。复活变体在对抗反派下显现难度作用（正派胜率再降 8~10 个百分点，403 给反派多了一个稀释位）。

**耗时**：normal 反派 54~209ms/局；hard 反派 938~1263ms/局（≈200~250ms/决策，单机场景可接受）。

### 5.2 验收结论

- **胜率回落达标**：正派胜率从 86~93% 回落到 38~69% 区间；normal vs hard、hard vs hard、复活 hard vs hard 均落在 40~60% 健康区，其余配对随难度梯度合理分布；
- **难度梯度清晰**：vs easy/normal/hard 反派 = 89% / 62% / 50%（normal 正派），旧版三档几乎无差异；
- **R1 稀释与 R2 卡序从目标函数自然涌现**：命中率 1.7+ → 0.6~1.0 即证据；
- **"正派硬档更新反派模型"实验被证伪（2026-09-02）**：曾将 `good_ai._score_plan` 的击杀惩罚项替换为"反派对抗泄漏度"奖励项，期望 hard 正派"防对攻击"——实验数据反向：hard vs hard 胜率 48% → **14%**。原因：泄漏度度量与命中项**先天冲突**（未被监视的反派越多、响应暴露度越大 → 诱导正派主动放低命中）。已回退（`lambda_kill` 恢复，代码注释留存实验记录）。结论：hard 正派 vs hard 对抗反派 ≈ 48% 即为当前信息结构下的健康均衡，无需"防隐身"改造——反派的稀释压低的正是正派后验的信息质量，属于游戏性设计的正常代价。

### 5.3 难度映射（实测支撑）

| 单人模式难度 | revive403 | 反派档 | 正派玩家实测胜率（normal/hard 正派） |
|---|---|---|---|
| 简单 | off | easy | 89~91% |
| 普通 | off | normal | 62~69% |
| 困难 | on | normal | 52~60% |
| 困难+ | on | hard | 38~44% |

---

## 6. 与智能体化的衔接

- 反派 AgentBrain 的提示词直接复用 **R1~R6**（对应正派提示词中的 P1~P8）；
- 反派智能体的内置工具 = 正派视角模拟推断器（heatmap + 全局复盘一致性检验 `replay_check`），即 §3 目标函数的可解释版本；
- 反派数据集标注的"最优动作"判定标准同样以 `evil_score` + 满员规则为硬标准（对应正派侧的排除法一票否决）。

---

*本文档与 `human-knowledge/good-principles.md` 互为镜像：正派文档记录"如何解题"，本文档记录"如何让解题者解不出"。*
