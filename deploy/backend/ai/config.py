"""
AI 全局配置 —— 权重与难度参数集中管理，便于回放调参。

各权重含义见对应模块注释：
- ALPHA:             正派贝叶斯推断的软似然强度（站位线索权重）
- EVIL_WEIGHTS:      反派 AI 评分函数权重
- GOOD_HARD_WEIGHTS: 正派困难档方案评分权重
"""

# 正派贝叶斯推断
ALPHA = 2.0

# 几何站位约束模式（2026-09-01 规则勘误后新增）：
#   True  = 硬约束：反派必须站在自己标记的影响范围内（与前端/引擎规则一致），
#           不满足"每个标记都能指派一名范围内反派"的组合直接剔除；
#   False = 软似然（旧行为）：范围线索只作加权，容忍越范围标记。
GEOMETRY_HARD = True

# 反派 AI 评分权重
EVIL_WEIGHTS = {
    'kill_gain': 1.0,         # 击杀正派基础收益
    'lambda_purge': 0.5,      # 击杀"正派误判的高嫌疑角色"→ 帮正派排除，扣分系数
    'lambda_exposure': 1.5,   # 暴露度惩罚系数
    'tempo': 1.0,             # 节奏因子
    'utilization': 5.0,       # 行动利用率（惩罚浪费行动次数）
}

# 正派困难档方案评分权重
GOOD_HARD_WEIGHTS = {
    'lambda_spread': 0.05,   # 分散站位权重（防止反派"声东击西"）
    'lambda_kill': 1.0,      # 压制反派击杀收益的权重。
                             # 注（2026-09-02 实验记录）：曾替换为"对抗泄漏度"奖励项，
                             # 实验证伪（hard 胜率 48%→14%，泄漏度与命中项先天冲突），
                             # 已回退为击杀收益惩罚。
}

# 反派 AI 对抗评分权重（倒推设计 v1，2026-09-02）：
#   反派唯一课题 = 被迫行动下的泄漏最小化（标记对反派是纯信息损失）。
#   最终精确评分 = 模拟正派后验后的真反派暴露度之和（exposure）。
#   代理评分（proxy_*）用于快速筛选候选，是可解释的 R1/R6 镜像。
EVIL_ADVERSARIAL_WEIGHTS = {
    'exposure': 1.0,      # 精确评分：真反派暴露度之和（越低越好）
    'proxy_purge': 2.0,   # 代理评分：标记高嫌疑无辜者的惩罚（防误杀，帮正派排除）
    'proxy_buddy': 1.5,   # 代理评分：范围内其他可行动真反派的惩罚（防连带暴露同伙）
    'proxy_unique': 4.0,  # 代理评分：自己是范围内唯一可行动反派的重罚（防被精确定位）
}
